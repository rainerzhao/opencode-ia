import React,{useEffect,useState}from'react';import{request}from'../../api/client';export function SkillsPage(){const[items,setItems]=useState([]);useEffect(()=>{request('/api/skills').then(setItems)},[]);return <section className="panel"><h3>团队 Skill 资产</h3><p className="muted">Stage 4 将补齐校验、发布、安装和回滚。</p><div className="list">{items.length?items.map(s=><article key={s.name}><b>{s.name}</b><span>{s.description||'暂无描述'}</span></article>):<div className="empty">暂无已安装 Skill</div>}</div></section>}
