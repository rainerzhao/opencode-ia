import React,{useEffect,useMemo,useState}from'react';import{request}from'../../api/client';
function label(item){return item.status==='published'?'团队已发布':item.status==='withdrawn'?'已撤回为私有':'私有草稿';}
function Confirmation({target,onCancel,onConfirm,busy}){if(!target)return null;return <div className="modal-backdrop"><section className="modal" role="dialog" aria-modal="true" aria-label="确认内容状态"><p className="eyebrow">需要人工确认</p><h3>{target.action==='publish'?'发布到团队？':'撤回为私有？'}</h3><p className="muted">{target.action==='publish'?'发布后团队成员可以搜索和查看此版本。':'撤回后团队成员将不能继续访问此内容。'}</p><div className="modal-actions"><button className="ghost" type="button" onClick={onCancel}>取消</button><button className={target.action==='publish'?'publish':'danger'} type="button" onClick={onConfirm} disabled={busy}>确认{target.action==='publish'?'发布':'撤回'}</button></div></section></div>}
export function KnowledgePage({initialEditor=null,initialItems=[]}){const[items,setItems]=useState(initialItems),[query,setQuery]=useState(''),[editor,setEditor]=useState(initialEditor),[notice,setNotice]=useState({text:'',error:false}),[pending,setPending]=useState(null),[busy,setBusy]=useState(false);const load=async()=>{try{setItems((await request('/api/content/knowledge')).knowledge||[]);}catch(error){setNotice({text:error.message,error:true});}};useEffect(()=>{load();},[]);useEffect(()=>{const q=query.trim();if(!q)return;let active=true;const timer=window.setTimeout(async()=>{try{const body=await request(`/api/content/knowledge/search?q=${encodeURIComponent(q)}`);if(active)setItems(body.knowledge||[]);}catch(error){if(active)setNotice({text:error.message,error:true});}},250);return()=>{active=false;window.clearTimeout(timer);};},[query]);const visible=useMemo(()=>items,[items]);async function open(item){try{const body=await request(`/api/content/knowledge/${encodeURIComponent(item.id)}`);setEditor({...body.knowledge,isNew:false,content:body.knowledge.markdown});}catch(error){setNotice({text:error.message,error:true});}}async function save(event){event.preventDefault();const form=new FormData(event.currentTarget);setBusy(true);try{const payload={title:form.get('title'),category:form.get('category'),tags:String(form.get('tags')||'').split(',').map(v=>v.trim()).filter(Boolean),markdown:form.get('content')};const body=editor?.isNew?await request('/api/content/knowledge',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)}):await request(`/api/content/knowledge/${encodeURIComponent(editor.id)}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});setEditor(null);setNotice({text:`文档已保存为${label(body.knowledge)}。`,error:false});await load();}catch(error){setNotice({text:error.message,error:true});}finally{setBusy(false);}}async function transition(){if(!pending)return;setBusy(true);try{const body=await request(`/api/content/knowledge/${encodeURIComponent(pending.id)}/${pending.action}`,{method:'POST'});setPending(null);setNotice({text:pending.action==='publish'?'已发布到团队。':'已撤回为私有草稿。',error:false});await load();if(editor?.id===body.knowledge.id)setEditor({...editor,...body.knowledge});}catch(error){setNotice({text:error.message,error:true});}finally{setBusy(false);}}if(editor)return <section className="panel editor-panel"><div className="section-head"><div><p className="eyebrow">{editor.isNew?'新建私有文档':label(editor)}</p><h3>{editor.isNew?'撰写 Markdown 文档':editor.title}</h3></div><button className="ghost" onClick={()=>setEditor(null)}>返回列表</button></div><form className="stack" onSubmit={save}><label>标题<input name="title" defaultValue={editor.title} readOnly={!editor.isNew&&!editor.id} required/></label><label>分类<input name="category" defaultValue={editor.category||''} placeholder="如：GPU 选型"/></label><label>标签（逗号分隔）<input name="tags" defaultValue={(editor.tags||[]).join(', ')} placeholder="如：H800, 容量"/></label><label>正文<textarea className="knowledge-content" name="content" defaultValue={editor.content||''} placeholder="使用 Markdown 编写正文" required/></label><div className="form-actions"><span className="muted">保存后默认仅本人可见；发布必须单独确认</span><button disabled={busy}>{busy?'保存中…':'保存私有草稿'}</button></div></form>{!editor.isNew&&<div className="form-actions content-actions"><button className={editor.status==='published'?'ghost danger':'publish'} type="button" onClick={()=>setPending({id:editor.id,action:editor.status==='published'?'withdraw':'publish'})} disabled={busy}>{editor.status==='published'?'撤回团队':'发布到团队'}</button></div>}<Confirmation target={pending} onCancel={()=>setPending(null)} onConfirm={transition} busy={busy}/></section>;return <section className="panel"><div className="section-head knowledge-head"><div><h3>知识库</h3><p className="muted">新内容默认私有；只有人工发布后才成为团队知识。</p></div><div className="row-actions"><label className="button ghost file-button">上传文件<input type="file" multiple accept=".md,.txt,.docx,.pdf,.json,.csv" disabled/></label><button onClick={()=>setEditor({title:'',category:'',tags:[],content:'',isNew:true})}>新建文档</button></div></div><div className="knowledge-toolbar"><input type="search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="搜索标题或正文" aria-label="搜索知识库"/><span className="muted">{visible.length} 篇文档</span></div><p className={notice.error?'notice error':'notice success'} role="status">{notice.text}</p><div className="list knowledge-list">{visible.length?visible.map(item=><article className="content-card" key={item.id}><button className="knowledge-item" onClick={()=>open(item)}><b>{item.title}</b><span>{item.preview||item.category||'Markdown 文档'}</span><small>{label(item)} · v{item.version}</small></button><button type="button" className={item.status==='published'?'ghost danger':'publish'} onClick={()=>setPending({id:item.id,action:item.status==='published'?'withdraw':'publish'})} disabled={busy}>{item.status==='published'?'撤回团队':'发布到团队'}</button></article>):<div className="empty">{query?'没有匹配的知识文档':'暂无版本化知识文档'}</div>}</div><Confirmation target={pending} onCancel={()=>setPending(null)} onConfirm={transition} busy={busy}/></section>}
