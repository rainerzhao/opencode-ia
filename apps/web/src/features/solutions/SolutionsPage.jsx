import React,{useEffect,useState}from'react';import{request}from'../../api/client';
const label=item=>item.status==='published'?'团队已发布':item.status==='withdrawn'?'已撤回为私有':'私有草稿';
function Confirmation({target,onCancel,onConfirm,busy}){if(!target)return null;return <div className="modal-backdrop"><section className="modal" role="dialog" aria-modal="true" aria-label="确认方案状态"><p className="eyebrow">需要人工确认</p><h3>{target.action==='publish'?'发布到团队？':'撤回为私有？'}</h3><p className="muted">{target.action==='publish'?'发布后团队成员可以查看此方案。':'撤回后团队成员不能继续查看此方案。'}</p><div className="modal-actions"><button type="button" className="ghost" onClick={onCancel}>取消</button><button type="button" className={target.action==='publish'?'publish':'danger'} onClick={onConfirm} disabled={busy}>确认{target.action==='publish'?'发布':'撤回'}</button></div></section></div>}
export function SolutionsPage({initialItems=[]}){const[items,setItems]=useState(initialItems),[editor,setEditor]=useState(null),[notice,setNotice]=useState({text:'',error:false}),[pending,setPending]=useState(null),[busy,setBusy]=useState(false);const load=async()=>{try{setItems((await request('/api/content/solutions')).solutions||[]);}catch(error){setNotice({text:error.message,error:true});}};useEffect(()=>{load();},[]);async function open(item){try{const body=await request(`/api/content/solutions/${encodeURIComponent(item.id)}`);setEditor({...body.solution,isNew:false});}catch(error){setNotice({text:error.message,error:true});}}async function save(event){event.preventDefault();const f=new FormData(event.currentTarget);setBusy(true);try{const payload={title:f.get('title'),description:f.get('description'),solutionMarkdown:f.get('solutionMarkdown')};const body=editor?.isNew?await request('/api/content/solutions',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)}):await request(`/api/content/solutions/${encodeURIComponent(editor.id)}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});setEditor(null);setNotice({text:`方案已保存为${label(body.solution)}。`,error:false});await load();}catch(error){setNotice({text:error.message,error:true});}finally{setBusy(false);}}async function transition(){if(!pending)return;setBusy(true);try{await request(`/api/content/solutions/${encodeURIComponent(pending.id)}/${pending.action}`,{method:'POST'});setPending(null);setNotice({text:pending.action==='publish'?'方案已发布到团队。':'方案已撤回为私有草稿。',error:false});setEditor(null);await load();}catch(error){setNotice({text:error.message,error:true});}finally{setBusy(false);}}if(editor)return <section className="panel editor-panel"><div className="section-head"><div><p className="eyebrow">{editor.isNew?'新建私有方案':label(editor)}</p><h3>{editor.isNew?'撰写方案':editor.title}</h3></div><button className="ghost" onClick={()=>setEditor(null)}>返回列表</button></div><form className="stack" onSubmit={save}><label>方案标题<input name="title" defaultValue={editor.title} required/></label><label>问题背景与约束<textarea name="description" defaultValue={editor.description||''}/></label><label>方案正文<textarea className="knowledge-content" name="solutionMarkdown" defaultValue={editor.solutionMarkdown||''} required/></label><div className="form-actions"><span className="muted">保存为私有草稿，发布须单独确认</span><button disabled={busy}>{busy?'保存中…':'保存私有草稿'}</button></div></form>{!editor.isNew&&<div className="form-actions content-actions"><button type="button" className={editor.status==='published'?'ghost danger':'publish'} onClick={()=>setPending({id:editor.id,action:editor.status==='published'?'withdraw':'publish'})}>{editor.status==='published'?'撤回团队':'发布到团队'}</button></div>}<Confirmation target={pending} onCancel={()=>setPending(null)} onConfirm={transition} busy={busy}/></section>;return <section className="panel"><div className="section-head"><div><h3>我的需求方案</h3><p className="muted">方案默认私有，人工发布后才供团队复用。</p></div><button onClick={()=>setEditor({title:'',description:'',solutionMarkdown:'',isNew:true})}>新建方案</button></div><p className={notice.error?'notice error':'notice success'} role="status">{notice.text}</p><div className="list">{items.length?items.map(item=><article className="content-card" key={item.id}><button className="knowledge-item" onClick={()=>open(item)}><b>{item.title}</b><span>{label(item)} · v{item.version}</span></button><button type="button" className={item.status==='published'?'ghost danger':'publish'} onClick={()=>setPending({id:item.id,action:item.status==='published'?'withdraw':'publish'})} disabled={busy}>{item.status==='published'?'撤回团队':'发布到团队'}</button></article>):<div className="empty">暂无版本化需求方案</div>}</div><Confirmation target={pending} onCancel={()=>setPending(null)} onConfirm={transition} busy={busy}/></section>}
