import React,{useState}from'react';import{api}from'../../api/client';
export function LoginPage({onLogin}){const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);setError('');const data=new FormData(e.currentTarget);try{const r=await api.login(data.get('username').trim(),data.get('password'));onLogin(r.user);}catch(err){setError(err.status===429?'尝试次数过多，请稍后再试。':'用户名或密码不正确。');}finally{setBusy(false);}}return <main className="login"><section className="login-copy"><span className="mark">OC</span><p className="eyebrow">团队统一入口</p><h1>OpenCode 团队<br/>AI 工作台</h1><p>统一组织模型、Agent、Skill 与团队知识资产。每一次操作都有账号归属，个人内容默认私有。</p></section><section className="login-card"><p className="eyebrow">欢迎回来</p><h2>登录工作台</h2><p className="muted">使用管理员分配的团队账号。</p><form onSubmit={submit}><label>用户名<input name="username" autoComplete="username" required autoFocus/></label><label>密码<input name="password" type="password" autoComplete="current-password" required/></label><p className="error" role="alert">{error}</p><button disabled={busy}>{busy?'正在登录…':'登录'}</button></form></section></main>}
