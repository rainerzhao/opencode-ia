import React,{useEffect,useState} from 'react';import{createRoot}from'react-dom/client';import{api}from'./api/client';import{LoginPage}from'./features/auth/LoginPage';import{WorkbenchShell}from'./shell/WorkbenchShell';import'./styles.css';
function App(){const[user,setUser]=useState(null);const[ready,setReady]=useState(false);useEffect(()=>{const expire=()=>setUser(null);window.addEventListener('workbench:auth-expired',expire);api.me().then(r=>setUser(r.user)).catch(()=>{}).finally(()=>setReady(true));return()=>window.removeEventListener('workbench:auth-expired',expire);},[]);if(!ready)return <div className="boot">正在验证身份…</div>;if(!user)return <LoginPage onLogin={setUser}/>;return <WorkbenchShell user={user} onLogout={async()=>{try{await api.logout();}finally{setUser(null);}}}/>;}createRoot(document.getElementById('root')).render(<App/>);
