# P1A 实施记录

## 2026-09-13：输入契约

- 新 App Goal 已生效，移除仓库中等待修改目标的旧说明。
- 新增需求、沟通、分页/筛选输入契约与测试。拒绝客户端所有者/共享字段；限定状态、来源、长度；验证带时区的真实日期；保留原始沟通正文。
- RED：运行 `node --test test/requirements/requirement-input.test.js`，5 个测试因输入契约缺失失败。
- GREEN：实现后同命令 5 pass、0 fail。此结果仅验证输入层，不证明数据库、HTTP 权限或完整 P1A 已完成。
- 已完成：SQLite/MySQL 迁移 v10、双仓储、受认证 REST 路由、生产 MySQL 组合、审计元数据收敛。管理员仅能维护 BU，无法因角色读取私人需求正文。
- 已验证：SQLite 输入/仓储/API 测试；MySQL 真库迁移、持久化与 HTTP 私有生命周期；全量回归。提交前的完整证据见 `04-acceptance-report.md`。

## 内联评审

- 输入身份字段拒绝不能代替仓储权限检查；后续 SQL 必须按认证账号限定所有读写。
- 沟通原文保留空白；需求描述作为编辑字段可规范化。
- 此次未修改现有内容模块的管理员访问语义；跨模块权限统一仍需验收。
