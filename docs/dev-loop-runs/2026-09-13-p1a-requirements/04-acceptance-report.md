# P1A 需求与沟通底座：验收报告

验收日期：2026-09-14

## Verdict

PASS_WITH_NOTES

## 范围

- MySQL/SQLite v10–v11：BU、需求、原始沟通记录、首任负责人及索引；既有迁移保持追加式。
- 受认证 API：BU 读取与管理员维护，需求创建/读取/更新/归档（`status=archived`）/分页筛选，原始沟通追加。
- 默认私有：所有需求正文查询以 `owner_user_id` 限定；管理员角色不扩大此权限；审计不写沟通正文。
- 正式 MySQL 生产组合已经实例化需求仓储，SQLite/Demo 也具备同一功能底座。

## 验证证据

| 验证 | 结果 | 说明 |
| --- | --- | --- |
| 输入、SQLite 仓储、HTTP 单元/集成 | 11 pass, 0 fail | 覆盖伪造所有者/公开字段、日期、越权 404、管理员 BU 权限和安全审计 |
| MySQL 真库专项 | 3 pass, 0 fail | 回环专用 MySQL 8.4：v10 可重复迁移、私有持久化、真实 HTTP 生命周期 |
| 全量回归 | 372 pass, 0 fail, 7 skip | `WORKBENCH_TEST_MYSQL_URL=… npm test`；7 个 skip 是既有 opt-in 真实 OpenCode 场景，不是本阶段失败 |
| 语法检查 | 190 files passed | `npm run check` |

## 需求覆盖

- BU、统一核心字段和首任负责人：已完成；负责人默认是创建者，跨人转交需显式授权流程；受控自定义字段属于 P1C。
- IIM/电话/会议手工纪要：已完成，`channel` 明确标识来源，原文与 AI/人工结论没有混写。
- 默认私有、持久化、分页筛选、审计：已完成并验证。
- 负责人：第一版固化为创建者；跨人转交/共享需显式授权模型，尚未实现。

## 未完成但非 P1A 出口

- 前端“需求与场景”页面、工作台概览及浏览器验收：P2A。
- 需求与 Conversation、知识、方案之间的关联，完整历史分页/搜索/归档：P1B。
- 自定义字段模板、OpenCode 生成草稿及人工确认：P1C。
- 公司内网 Linux、内部 Provider、20 人真实容量与灾备：P3B。

## 审查结论

未发现阻塞或重要问题。风险边界是：当前 API 已可用但尚无正式前端入口，因此不得对使用者宣称“需求与场景页面已上线”。

## 发布记录

P1A 主提交 `b1e1034` 已推送至 `origin/main`，远端分支 SHA 已于 2026-09-14 核验。
