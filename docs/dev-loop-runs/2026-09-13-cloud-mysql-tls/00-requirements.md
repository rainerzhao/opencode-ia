# 云数据库 TLS

## Goal

公司云 MySQL 使用内部 CA 时，应用、管理员初始化和备份恢复采用相同证书链与 DNS 主机名校验规则。TLS 不可因客户端默认配置或无效 CA 静默降级。

## Baseline and scope

main，e4d22e434ef7356a936b40fb90f5f98260488b03。沿用自动开发和分阶段提交授权。旧未跟踪材料不在范围内。公司数据库实际证书、地址与凭证不进入仓库；测试使用临时生成的证书和隔离 MySQL。
