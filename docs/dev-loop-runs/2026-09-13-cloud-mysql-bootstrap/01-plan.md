# 实施与验收

1. 用真实 CLI 子进程验证缺少配置、混用配置及 MySQL 不可达时禁止 SQLite 回退。
2. CLI 按服务配置选择数据库，检查 MySQL 能力并迁移，复用异步身份仓储。用既有数据库 advisory lock 将首次初始化串行化，用户和审计仍在同一事务内。
3. 在隔离真库启动两个 CLI，验证只有一个管理员和一条初始化审计。
4. 生产组合集成测试改用 CLI 创建账号，再执行既有 HTTP/WebSocket 流程。
5. 更新 README 与操作命令，执行全量真库、构建、语法和密钥检查。
